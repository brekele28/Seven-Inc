import {
    socials,
    addresses,
    contacts,
} from "../data/footerData";

export default function footerRepository() {
    return {
        socials,
        addresses,
        contacts,
    };
}